# CLI 入口组件 (`cli.ts`)

### 💡 核心思想
作为命令行统一入口，负责解析终端输入并分发至各子命令执行函数。保持入口轻量、扁平与纯粹，绝不在此堆叠业务逻辑，仅负责参数解析与子系统唤起。

### 💻 使用示例
```bash
# 查看全局帮助与全部 5 个命令
node dist/cli.js --help

# 启动单轮调试模式
node dist/cli.js once --dry-run
```
