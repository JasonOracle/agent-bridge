# 产品需求文档 — Todo App

## 1. 核心目标
构建一个纯原生 Node.js 的轻量级待办事项管理引擎，支持增删改查、状态过滤、统计以及 JSON 序列化持久化导入导出。

## 2. 功能清单
- T1: 定义 TodoItem 结构与 TodoList 核心容器类
- T2: 支持添加待办条目（自动分配自增/唯一 ID）与按 ID 删除
- T3: 支持待办事项状态切换（pending / done）与内容编辑
- T4: 支持按状态过滤待办列表与统计各状态数量
- T5: 支持全量列表与 JSON 字符串互相转换
